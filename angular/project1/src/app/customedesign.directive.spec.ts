import { CustomedesignDirective } from './customedesign.directive';

describe('CustomedesignDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomedesignDirective();
    expect(directive).toBeTruthy();
  });
});
